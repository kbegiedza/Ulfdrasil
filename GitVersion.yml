mode: Mainline
branches:
  main:
    regex: ^main$
    increment: Patch
    track-merge-target: true
  master:
    regex: ^master$
    increment: Patch
    track-merge-target: true

tag-prefix: 'v'

ignore:
  sha: []
